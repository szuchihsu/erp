<% content_for :title, "Add Item to Order #{@sales_order.order_id}" %>

<div class="mx-auto md:w-2/3 w-full">
  <div class="mx-auto">
    <h1 class="font-bold text-4xl">Add Item to Order</h1>
    <p class="mb-4 text-gray-600">Order: <%= @sales_order.order_id %> - <%= @sales_order.customer.name %></p>

    <%= render "form", sales_order_item: @sales_order_item %>
  </div>
</div>
